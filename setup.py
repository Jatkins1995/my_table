#!/usr/bin/python

from setuptools import setup

setup(name='my_table',
      version='0.1',
      description='Base code mor my_table',
      author='Jason Atkins',
      license='MIT',
      packages=['my_table'],
      zip_safe=False)
